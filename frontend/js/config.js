/* ============================================================
   OrbitAI — config.js
   Preencha com as credenciais do seu projeto Supabase
   (Project Settings → API). A anon key é segura para o
   browser — o acesso é controlado por RLS no banco.
   ============================================================ */
window.ORBIT_CONFIG = {
  SUPABASE_URL: 'https://SEU-PROJETO.supabase.co',
  SUPABASE_ANON_KEY: 'SUA-ANON-KEY',
};
